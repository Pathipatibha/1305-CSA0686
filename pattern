#include<stdio.h>
int main()
{
	int n,i,j;
	printf(" enter the rows needed:");
	scanf("%d",&n);
	if(n<0)
	    printf(" inavalid output");
	else
	{
		for(i=0;i<n;i++)
		{
			for(j=0;j<=i;j++)
			{
				printf(" \n%d ",j+1);
			}
			printf(" \n");
		}
	}
	return 0;
}
